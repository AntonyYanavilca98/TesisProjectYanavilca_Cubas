<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comentarios</title>
</head>

<body>
    <form method="POST" action="/producto/comentarios/<%= data._id %>">
        <p>logo</p>
        <% if( usuario.image === null ) { %>
            <img src="/images/profile-default.png" width="400px" height="400px" />
            <br/>
            <% } else if ( usuario.image > 0 ){ %>
                <img src="https://imageuploadcv.s3.us-east-2.amazonaws.com/<%= usuario.ruta %>" width="400px" height="400px" />
                <br/>
                <% } else { %>
                    <img src="<%= usuario.image %>" width="400px" height="400px" />
                    <br/>
                    <% } %>
                        <img src="" alt="">
                        <label>Nombre</label>
                        <p>
                            <%= nombreUsuario %>
                        </p>
                        <label>Descripcion</label>
                        <input type="text" name="descripcion">
                        <p>-----------------------------</p>
                        <input type="submit" value="Comentar">
    </form>
    <form>

        <% for (var i= 0; i < comment.length; i++){ %>
            <% if ( id == comment[i].producto   ) { %>

                <p>Logo</p>
                <% if( comment[i].usuario.image === null ) { %>
                    <img src="/images/profile-default.png" width="400px" height="400px" />
                    <br/>
                    <% } else if ( comment[i].usuario.image > 0 ){ %>
                        <img src="https://imageuploadcv.s3.us-east-2.amazonaws.com/<%= comment[i].usuario.ruta %>" width="400px" height="400px" />
                        <br/>
                        <% } else { %>
                            <img src="<%= comment[i].usuario.image %>" width="400px" height="400px" />
                            <br/>
                            <% } %>



                                <p>Nombre</p>
                                <p>
                                    <%= comment[i].usuario.nombres %>
                                </p>
                                <p>descripcion</p>
                                <%= comment[i].descripcion %>
                                    <br/> -----------------------
                                    <% } %>

                                        <% } %>
    </form>
</body>

</html>